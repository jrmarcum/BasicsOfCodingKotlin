Uses `java.net.http.HttpClient` (JDK 11+); response body content may vary if the website changes.

___

##### Run Command:
`kotlinc http-client.kt -include-runtime -d http-client.jar && java -jar http-client.jar`

##### Results:
`Response status: 200 OK`
`<!DOCTYPE html>`
`<html>`
`  <head>`
`    <meta charset="utf-8">`
`    <title>Go by Example</title>`
