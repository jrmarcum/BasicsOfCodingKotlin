Kotlin uses `sortWith(compareBy { ... })` in place of implementing `sort.Interface`.

___

##### Run Command:
`kotlinc sorting-by-functions.kt -include-runtime -d sorting-by-functions.jar && java -jar sorting-by-functions.jar`

##### Results:
`[kiwi peach banana]`
